# Organization deployment: what was checked on 2026-09-20

The checks behind the verification-status table in `docs/DEPLOY_ORG.md`, all on one macOS machine. The admin-managed files were placed by the repository owner with `sudo deploy/org/install.sh --server-url https://mcp.catpilot.ai/mcp`; the sessions below were run afterwards as a normal user. The public endpoint carries no overlay, so `unknown_policy: true` is the expected answer everywhere.

## Codex CLI 0.154.0: user-level skills directory and `mcp_servers` in `config.toml`

Setup: a temporary `HOME` holding only a copy of the credentials file, `~/.agents/skills/catpilot-safe-building/` copied from `skills/`, and this `~/.codex/config.toml`:

```toml
model = "gpt-5.6-terra"
model_reasoning_effort = "low"

[mcp_servers.catpilot_guardrails]
url = "https://mcp.catpilot.ai/mcp"
```

Command, from an empty directory, `CODEX_HOME` unset, stdin closed (`codex exec` waits on an open stdin):

```bash
codex exec --json --skip-git-repo-check "Answer in three short lines. 1) What guidance skills do you have available, by name? 2) Call the catpilot_guardrails MCP tool list_approved with category \"hosting\" and report the first approved item and the unknown_policy value. 3) Say which skill file you read, if any." < /dev/null
```

Observed in the JSON event stream: the model named `catpilot-safe-building`, ran `sed -n '1,240p' <home>/.agents/skills/catpilot-safe-building/SKILL.md`, and a `mcp_tool_call` item for `catpilot_guardrails` / `list_approved` with `{"category": "hosting"}` completed with the public endpoint's generic hosting item and `unknown_policy: true`, which is the expected answer from the endpoint that never carries an overlay. Usage: 65,750 input tokens (56,320 cached), 315 output. Second run, after `install.sh`, in a fresh temporary home holding only the credentials file and a `config.toml` with just the model and effort, no skills directory and no `mcp_servers` entry: the model listed `catpilot-safe-building` and `catpilot-security-core` "from `/private/etc/codex/skills`" (macOS resolves `/etc` there), and `list_approved` completed through `catpilot_guardrails`, which only `/etc/codex/managed_config.toml` named. Usage: 44,960 input tokens (37,120 cached), 360 output. So `/etc/codex/skills/` and `/etc/codex/managed_config.toml` are read, and their keys are the user-level ones.

## Claude Code 2.1.241: the managed settings file, the managed skills directory, and `managedMcpServers`

**Testing variable, before the files were placed.** A `managed-settings.json` outside the system path, referenced through `CLAUDE_CODE_MANAGED_SETTINGS_PATH` (the vendor's environment-variable reference describes it as a testing path that replaces the system file), with both hooks and a `managedMcpServers` entry. Command: `claude -p "<three-step prompt>" --model claude-haiku-4-5-20251001 --permission-mode bypassPermissions --max-turns 12 --output-format stream-json --verbose`. Observed: the `init` event listed no managed server, the Bash command with the example key ran, the `key.pem` write succeeded, no evidence file appeared. The variable did not take effect on this version.

**System path, after `install.sh`.** `deploy/org/verify.sh` found every file present and the managed settings file parsing as JSON. First session on Haiku with the same three-step prompt under `--permission-mode bypassPermissions`: the model declined the whole task on its own judgment before any tool call, so nothing was exercised; recorded here because a refusal is not a hook. Second session, prompt reframed as a hook verification with documentation-only values, `--allowedTools "Bash,Write" --max-turns 8`:

- `Bash` with `echo AKIAIOSFODNN7EXAMPLE`: tool result was the hook's reason, "this command appears to contain a literal credential (AWS access key ID)…", and the command did not run.
- `Write` of `fixture.pem` with a PEM header line and `SYNTHETIC-NOT-A-KEY`: tool result was the hook's reason, "A literal private-key block was found in the content being written…", and no file was written.
- `Bash` with `echo done`: ran, printed `done`.
- `~/.catpilot-guardrails/evidence.jsonl` received two lines, `{"event": "deny", "hook": "pretooluse-secrets", "label": "AWS access key ID", "session_id": "<id>", "source": "claude-code-hook", "tool": "Bash", "ts": "…"}` and the `pretooluse-write-private-key` counterpart with `"tool": "Write"`, and no command or content text. The `$HOME` in the hook command line expanded, so the managed command line is what ran.

Rule-outs: `~/.claude/settings.json` has no `hooks` key, `~/.claude/skills/` holds only Anthropic's synced skills, and the working directory has no `.claude/`. Cost of the two sessions: about $0.16.

**Managed skills directory.** A third session, `--max-turns 2`, asked only to list available skills by name: the `init` event's skill list and the reply both contained `catpilot-safe-building` and `catpilot-security-core`, with no user-level or project copy present. Cost: $0.02.

**`managedMcpServers`.** `claude mcp list` after the file was placed showed only the claude.ai connector, and no session's `init` event listed `catpilot-guardrails`. The key is named in the 2.1.241 binary but did not load the server from the managed settings file. `managed-mcp.json` was not placed and is not yet exercised. The repository `.mcp.json` route and the `--mcp-config` route are in the tested-runtimes table from 2026-09-14.

## Evidence log

Not a host observation: `tests/test_hook.py`, `tests/test_write_hook.py`, and `tests/test_mcp_server.py` cover the recorded fields, the redaction of command, content, and description text, the `input_error` line on malformed input, and that an unwritable path leaves every decision and answer unchanged.
