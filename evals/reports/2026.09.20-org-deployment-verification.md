# Organization deployment: what was checked on 2026-09-20

Two checks behind the verification-status table in `docs/DEPLOY_ORG.md`. Both ran on one macOS machine; neither exercised an admin-managed system path, because writing there needs an administrator and the repository's rule is to record only what was observed.

## Codex CLI 0.154.0: user-level skills directory and `mcp_servers` in `config.toml`

Setup: a temporary `HOME` holding only a copy of the credentials file, `~/.agents/skills/catpilot-safe-building/` copied from `skills/`, and this `~/.codex/config.toml`:

```toml
model = "gpt-5.6-terra"
model_reasoning_effort = "low"

[mcp_servers.catpilot_guardrails]
url = "https://mcp.catpilot.ai/mcp"
```

Command, from an empty directory, `CODEX_HOME` unset, stdin closed (`codex exec` waits on an open stdin):

```bash
codex exec --json --skip-git-repo-check "Answer in three short lines. 1) What guidance skills do you have available, by name? 2) Call the catpilot_guardrails MCP tool list_approved with category \"hosting\" and report the first approved item and the unknown_policy value. 3) Say which skill file you read, if any." < /dev/null
```

Observed in the JSON event stream: the model named `catpilot-safe-building`, ran `sed -n '1,240p' <home>/.agents/skills/catpilot-safe-building/SKILL.md`, and a `mcp_tool_call` item for `catpilot_guardrails` / `list_approved` with `{"category": "hosting"}` completed with the public endpoint's generic hosting item and `unknown_policy: true`, which is the expected answer from the endpoint that never carries an overlay. Usage: 65,750 input tokens (56,320 cached), 315 output. Not exercised: `/etc/codex/managed_config.toml` and `/etc/codex/skills/`, which the 0.154.0 binary names as its admin paths.

## Claude Code 2.1.241: the managed-settings testing variable

Setup: a `managed-settings.json` outside the system path carrying both hooks (with `CATPILOT_EVIDENCE_LOG` set on each hook command) and a `managedMcpServers` entry for `https://mcp.catpilot.ai/mcp`, referenced through `CLAUDE_CODE_MANAGED_SETTINGS_PATH`, which the vendor's environment-variable reference describes as a testing path that replaces the system file.

Command: `claude -p "<three-step prompt: the AWS documentation-only example key in a Bash command, a key.pem write with a PEM header, a list_approved lookup>" --model claude-haiku-4-5-20251001 --permission-mode bypassPermissions --max-turns 12 --output-format stream-json --verbose`.

Observed: the session's `init` event listed no `catpilot-guardrails` server, the `Bash` command ran (`ok`), the `key.pem` write succeeded, and no evidence file was written; the model reported the server as unavailable. The managed file was not read through that variable on this version. Nothing here says the system path fails; it says the testing variable is not a substitute for it. The system-path check stays open until an administrator runs `deploy/org/install.sh` on a machine and repeats the three steps.

## Evidence log

Not a host observation: `tests/test_hook.py`, `tests/test_write_hook.py`, and `tests/test_mcp_server.py` cover the recorded fields, the redaction of command, content, and description text, the `input_error` line on malformed input, and that an unwritable path leaves every decision and answer unchanged.
